<%= form_for(@article.likes.find_by(article_id: @article.id),
             html: { method: :delete },
             remote: true) do |f| %>
<%= f.submit "Unlike", class: "btn btn-xs btn-warning" %>
<% end %>
